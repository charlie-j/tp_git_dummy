# --------------------------------------------------------------------
true : debug
true : warn_a
true : -traverse
true : bin_annot

# --------------------------------------------------------------------
<src> : include

# --------------------------------------------------------------------
<src/*.{ml,mli}> : package(batteries,num)
<tests/*.{ml,mli}> : package(batteries,num)
